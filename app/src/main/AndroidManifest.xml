<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.semesterprojekt.paf_quiz_client">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuizPatternsAndFrameworks">
        <activity
            android:name="de.semesterprojekt.paf_android_quiz_client.RegisterActivity"
            android:exported="false" />
        <activity
            android:name="de.semesterprojekt.paf_android_quiz_client.LoginActivity"
            android:exported="false" />
        <activity
            android:name="de.semesterprojekt.paf_android_quiz_client.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>